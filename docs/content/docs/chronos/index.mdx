---
title: Chronos
description: Chronos - Play with Time and Date, Be a Time Master like Chronos
icon: Clock
---

## `Chronos`

<Callout type="idea" title="About Chronos" >
In ancient Greek mythology, **Chronos** is the primordial embodiment of time — not merely tracking moments, but **defining their very existence**. Like its mythological namesake, the `Chronos` class offers **precise, immutable, and expressive control** over time within your application.

Designed to go beyond `Date`, it empowers you to manipulate, format, compare, and traverse time with **clarity, reliability, and confidence** — all while staying _immutable_ and _framework-agnostic_.

Whether you're building a calendar, a countdown, or scheduling logic, `Chronos` gives you the power to shape time as you see fit.
</Callout>

## API Reference for Chronos

This documentation provides a detailed guide to the `Chronos` class, a comprehensive date and time manipulation class. The methods are grouped into logical categories for easier navigation.

<Callout type="info">
For `chronos` function, a `Chronos` wrapper, refer to [**chronos**](wrapper-function)
</Callout>

### Import

```ts
import { Chronos } from 'chronos-date';
```

### Plugin System

Chronos supports a modular plugin system that allows you to extend its capabilities without bloating the core. Plugin methods are **not available by default**, you must explicitly install them using the `.use()` or `.register()` static method.

#### How it works

```ts
import { Chronos } from 'chronos-date';
import { seasonPlugin } from 'chronos-date/plugins/seasonPlugin';

// Register the plugin before using its methods
Chronos.use(seasonPlugin); 
// or
Chronos.register(seasonPlugin);

const now = new Chronos();
console.log(now.season()); // ✅ Safe to use after plugin registration
```

<Callout type="info">
Each plugin enhances the `Chronos` prototype and becomes available globally after registration.
</Callout>

### Constructor Signatures

```ts
 constructor();
 constructor(value: number);
 constructor(value: string);
 constructor(value: Date);
 constructor(value: Chronos);
 constructor(year: number, month: number, date?: number, hours?: number, minutes?: number, seconds?: number, ms?: number);
 constructor(value?: ChronosInput);
```

#### ChronosInput

```ts
type ChronosInput = number | string | Date | Chronos;
```

### Public & Protected Properties

These properties provide non-destructive, read-only (only the core ones) access to the copies of internal states of a `Chronos` instance for debugging, inspection, or meta-awareness.

<Callout type="info">
However, in JavaScript, these properties _can technically be mutated_ (Compile-time `Error` occurs in TypScript if these properties are tried to be mutated), but such mutations (changes) **do not** affect the `Chronos` instance itself. The class internally manages equivalent strict **readonly/private state**. These public properties exist _purely for developer convenience and sugar_.
</Callout>

#### native

Returns the underlying native JavaScript `Date` object used internally by the `Chronos` instance. This is useful for interoperability with APIs or libraries that expect a native `Date`.

<Callout type="info" title="Info">
Equivalent to [`toDate()`](conversion#todate) instance method.
</Callout>

```ts
const ch = new Chronos('2025-01-01');
console.log(ch.native.toISOString()); // → 2025-01-01T00:00:00.000Z
```

#### origin

Indicates how the current `Chronos` instance was created. This can be helpful for debugging flow logic or tracing method chains in complex date-time pipelines.

Possible values:

- `'root'`: if the instance was directly constructed
- A method name string such as `'addDays'`, `'startOf'`, etc., if the instance was produced as the result of a method call (which can create a new instance of `Chronos`). If no such method was called it shows the last previous method name as `origin`, if there is none, it shows `root`.

```ts
const root = new Chronos();
const viaMethod = root.addDays(3);

console.log(root.origin); // → 'root'
console.log(viaMethod.origin); // → 'addDays'
```

#### utcOffset

Returns the current UTC offset in `UTC±HH:mm` format (e.g., `"UTC+06:00"`, `"UTC-05:30"`).

<Callout type="info" title="Info">

- Also accessible via [`getTimeZoneOffset()`](extras#gettimezoneoffset) instance method without `UTC` prefix (returns in `±HH:mm` format).
- This value is automatically updated when using timezone or UTC manipulation methods.

</Callout>

```ts
const ch = new Chronos('2025-01-01');
console.log(ch.utcOffset); // → "UTC+06:00"

const ny = ch.timeZone('America/New_York');
console.log(ny.utcOffset); // → "UTC-05:00" (or UTC-04:00 during DST)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### timeZoneName

Represents the current timezone name (e.g., `"Bangladesh Standard Time"`), or falls back to the corresponding timezone identifier (e.g., `"Asia/Dhaka"`) if no name can be resolved.

<Callout type="info" title="Remarks">

- Invoking the [`timeZone()`](conversion#timezone) method sets the timezone name that corresponds to the specified UTC offset, or the UTC offset itself if no name exists. For more details on this behavior, see [`getTimeZoneName()`](names#gettimezonename).
- To retrieve the local system's native timezone name (or its identifier if the name is unavailable), use the [`$getNativeTimeZoneName()`](names#getnativetimezonename) instance method.

</Callout>

```ts
const ch = new Chronos('2025-01-01');
console.log(ch.timeZoneName); // → "Bangladesh Standard Time"

const custom = ch.timeZone('UTC+02:45');
console.log(custom.timeZoneName); // → "UTC+02:45" (no named timezone)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### timeZoneId

Represents the current timezone context, which can be a single identifier, an array of equivalent identifiers, or a UTC offset.

**Possible return types:**

- **`$TimeZoneIdentifier`** — e.g., `"Asia/Dhaka"`. Returned when the [`timeZone()`](conversion#timezone) method has not been invoked (default behavior).
- **Array of `$TimeZoneIdentifier`** — e.g., `['Asia/Kathmandu', 'Asia/Katmandu']`, used when multiple timezones share the same UTC offset such as `"UTC+05:45"`.
- **`UTCOffset`** — e.g., `"UTC+06:45"` or `"UTC+02:15"`, returned when no named timezone corresponds to a given offset.

<Callout type="info" title="Remarks">

- By default, when [`timeZone()`](conversion#timezone) is not applied, a single `$TimeZoneIdentifier` string is provided.
- When applied, it may instead return a single identifier string, an array of equivalent identifiers, or a UTC offset string.
- To retrieve the local system's native timezone identifier, use the [`$getNativeTimeZoneId()`](names#getnativetimezoneid) instance method.

</Callout>

```ts
const ch = new Chronos('2025-01-01');
console.log(ch.timeZoneId); // → "Asia/Dhaka" (system timezone)

const multi = ch.timeZone('UTC+05:30');
console.log(multi.timeZoneId); 
// → [ 'Asia/Calcutta', 'Asia/Colombo' ] (multiple equivalent zones)

const offsetOnly = ch.timeZone('UTC+02:15');
console.log(offsetOnly.timeZoneId); // → "UTC+02:15" (no named timezone)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### $tzTracker

<Callout type="error">
_**[Protected]**_ Internal flag used to track instances created via the [`timeZone()`](conversion#timezone) method. This value participates in the internal resolution of time-zone name, identifier, and offset. _Although it is protected_, it can still be mutated in JavaScript/TypeScript; and doing so may de-synchronize these time-zone properties and lead to inconsistent results. _Avoid modifying this field unless you fully understand the implications_.
</Callout>

### Available Methods

<details>
  <summary>[**Getter Methods**](getters)</summary>
    | Method                                     | Short Description                                                      |
    | ------------------------------------------ | ---------------------------------------------------------------------- |
    | [year](getters#year)                       | Gets the full year of the date.                                        |
    | [month](getters#month)                     | Gets the month (0-11) of the date.                                     |
    | [date](getters#date)                       | Gets the day of the month (1-31).                                      |
    | [weekDay](getters#weekday)                 | Gets the day of the week (0-6, where 0 is Sunday).                     |
    | [hour](getters#hour),                      | Gets the hour (0-23) of the date.                                      |
    | [minute](getters#minute)                   | Gets the minute (0-59) of the date.                                    |
    | [second](getters#second)                   | Gets the second (0-59) of the date.                                    |
    | [millisecond](getters#millisecond)         | Gets the millisecond (0-999) of the date.                              |
    | [isoWeekDay](getters#isoweekday)           | Gets ISO weekday: 1 = Monday, 7 = Sunday.                              |
    | [isoMonth](getters#isomonth)               | Gets ISO month (1–12 instead of 0–11).                                 |
    | [unix](getters#unix)                       | Returns the Unix timestamp (seconds since the Unix epoch).             |
    | [timestamp](getters#timestamp)             | Gets the time value in milliseconds since midnight, 1970-01-01 UTC.    |
    | [lastDateOfMonth](getters#lastdateofmonth) | Gets the last date (number) of the current month `(28, 29, 30 or 31)`. |
</details>

<details>
  <summary>[**Format Methods**](format)</summary>
    | Method                                | Short Description                                                         |
    | ------------------------------------- | ------------------------------------------------------------------------- |
    | [format()](format#format)             | Formats the current date into a custom string format.                     |
    | [formatStrict()](format#formatstrict) | Formats the date into a predefined strict string format.                  |
    | [formatUTC()](format#formatutc)       | Formats the date into a custom string format (UTC time).                  |
    | [calendar()](format#calendar)         | Returns a human-readable relative calendar time like `"Today at 3:00 PM"` |
    | [fromNow()](format#fromnow)           | Returns full time difference as string down to a given level.             |
    | [fromNowShort()](format#fromnowshort) | Returns a short human-readable string like "2h ago", "in 5m".             |
    | [getGreeting()](format#getgreeting)   | Returns a greeting message based on `Chronos` instance or provided time.  |
    | [greet()](format#alias)               | Alias for [`getGreeting()`](format#getgreeting)                           |
</details>

<details>
  <summary>[**Calculation Methods**](calculation)</summary>
    | Method                                           | Short Description                                                    |
    | ------------------------------------------------ | -------------------------------------------------------------------- |
    | [valueOf()](calculation#valueof)                 | Enables arithmetic and comparison operations (e.g., +new Chronos()). |
    | [add()](calculation#add)                         | Returns a new `Chronos` instance with the specified unit added.      |
    | [addDays()](calculation#alternatives)            | Adds days and returns a new immutable instance.                      |
    | [addHours()](calculation#alternatives)           | Adds hours and returns a new immutable instance.                     |
    | [addMinutes()](calculation#alternatives)         | Adds minutes and returns a new immutable instance.                   |
    | [addMonths()](calculation#alternatives)          | Adds months and returns a new immutable instance.                    |
    | [addSeconds()](calculation#alternatives)         | Adds seconds and returns a new immutable instance.                   |
    | [addWeeks()](calculation#alternatives)           | Adds weeks and returns a new immutable instance.                     |
    | [addYears()](calculation#alternatives)           | Adds years and returns a new immutable instance.                     |
    | [subtract()](calculation#subtract)               | Returns a new `Chronos` instance with the specified unit subtracted. |
    | [diff()](calculation#diff)                       | Returns the difference between current and another date.             |
    | [duration()](calculation#duration)               | Returns full time duration between current and another time object.  |
    | [durationString()](calculation#durationstring)   | Returns a human-readable formatted duration string between 2 dates.  |
    | [round()](calculation#round)                     | Rounds the current date-time to the nearest unit and interval.       |
    | [getDatesInRange()](calculation#getdatesinrange) | Generates a list of ISO date strings within a specified range.       |
</details>

<details>
  <summary>[**Name Getter Methods**](names)</summary>
    | Method                                                  | Short Description                                               |
    | ------------------------------------------------------- | --------------------------------------------------------------- |
    | [day()](names#day)                                      | Returns the name of the current day or optional day index.      |
    | [monthName()](names#monthname)                          | Returns the name of the current month or optional month index.  |
    | [$getNativeTimeZoneName()](names#getnativetimezonename) | Retrieves the local system's current timezone name.             |
    | [$getNativeTimeZoneId()](names#getnativetimezoneid)     | Retrieves the IANA time zone identifier for the local system.   |
    | [getTimeZoneName()](names#gettimezonename)              | Returns the current time zone name in descriptive string.       |
    | [getTimeZoneNameShort()](names#gettimezonenameshort)    | Returns the current time zone abbreviation (e.g. `"BST"`).      |
    | [getTimeZoneNameAbbr()](names#gettimezonenameshort)     | Get time zone abbreviation (Alias of `getTimeZoneNameShort()`). |
    | [season()](names#season)                                | Returns the current season name based on options.               |
    | [getSeasonName()](names#season)                         | Alias for [`season()`](names#season)                            |
    | [getZodiacSign()](names#getzodiacsign)                  | Returns the zodiac sign based on current date or `birthDate`.   |
    | [zodiac()](names#getzodiacsign)                         | Alias for [`getZodiacSign()`](names#getzodiacsign)              |
    | [getPartOfDay()](names#getpartofday)                    | Returns the part of day based on the current hour.              |
</details>

<details>
  <summary>[**Checker Methods**](checkers)</summary>
    | Method                                            | Short Description                                                    |
    | ------------------------------------------------- | -------------------------------------------------------------------- |
    | [isAfter()](checkers#isafter)                     | Checks if this date is after another date in a specific unit.        |
    | [isBefore()](checkers#isbefore)                   | Checks if this date is before another date in a specific unit.       |
    | [isBetween()](checkers#isbetween)                 | Checks if the current date is between the given start and end dates. |
    | [isSame()](checkers#issame)                       | Checks if another date is the same as this one in a specific unit.   |
    | [isSameOrAfter()](checkers#issameorafter)         | Checks if this date is same or after another in a specific unit.     |
    | [isSameOrBefore()](checkers#issameorbefore)       | Checks if this date is same or before another in a specific unit.    |
    | [isEqual()](checkers#isequal)                     | Checks if another date is exactly equal to this one.                 |
    | [isEqualOrAfter()](checkers#isequalorafter)       | Checks if another date is exactly equal to or after this one.        |
    | [isEqualOrBefore()](checkers#isequalorbefore)     | Checks if another date is exactly equal to or before this one.       |
    | [isBusinessHour()](checkers#isbusinesshour)       | Checks if the current date and time fall within business hours.      |
    | [isWeekend()](checkers#isweekend)                 | Checks if the current date falls on a weekend.                       |
    | [isWorkday()](checkers#isworkday)                 | Checks if the current date is a workday.                             |
    | [isFirstDayOfMonth()](checkers#isfirstdayofmonth) | Checks if current day is the first day of the current month.         |
    | [isLastDayOfMonth()](checkers#islastdayofmonth)   | Checks if current day is the last day of the current month.          |
    | [isToday()](checkers#istoday)                     | Checks if the current date is today.                                 |
    | [isTomorrow()](checkers#istomorrow)               | Checks if the current date is tomorrow.                              |
    | [isYesterday()](checkers#isyesterday)             | Checks if the current date is yesterday.                             |
    | [isLeapYear()](checkers#isleapyear)               | Checks if the current year is a leap year.                           |
    | [isPalindromeDate()](checkers#ispalindromedate)   | Checks if the current date (date part only) is a palindrome.         |
    | [isDST()](checkers#isdst)                         | Checks if the date is within daylight saving time (DST).             |
</details>

<details>
  <summary>[**Conversion Methods**](conversion)</summary>
    | Method                            | Short Description                                                            |
    | --------------------------------- | ---------------------------------------------------------------------------- |
    | [clone()](conversion#clone)       | Clones and returns exactly same `Chronos` instance.                          |
    | [toUTC()](conversion#toutc)       | Returns new `Chronos` instance in UTC time.                                  |
    | [toLocal()](conversion#tolocal)   | Returns new `Chronos` instance in local time.                                |
    | [timeZone()](conversion#timezone) | Creates a new instance of `Chronos` for the specified time zone information. |
    | [toDate()](conversion#todate)     | Gets the native `Date` instance.                                             |
    | [toArray()](conversion#toarray)   | Converts to array with all date unit part                                    |
    | [toObject()](conversion#toobject) | Converts to object with all date unit parts                                  |
</details>

<details>
  <summary>[**Comparison Methods**](comparison)</summary>
    | Method                                                        | Short Description                                    |
    | ------------------------------------------------------------- | ---------------------------------------------------- |
    | [compare()](comparison#compare)                               | Returns the difference of 2 dates in specified unit. |
    | [getRelativeDay()](comparison#getrelativeday)                 | Returns the number of days between 2 dates.          |
    | [getRelativeHour()](comparison#getrelativehour)               | Returns the number of hours between 2 dates.         |
    | [getRelativeMinute()](comparison#getrelativeminute)           | Returns the number of minutes between 2 dates.       |
    | [getRelativeSecond()](comparison#getrelativesecond)           | Returns the number of seconds between 2 dates.       |
    | [getRelativeMilliSecond()](comparison#getrelativemillisecond) | Returns the number of milliseconds between 2 dates.  |
    | [getRelativeWeek()](comparison#getrelativeweek)               | Returns the number of weeks between 2 dates.         |
    | [getRelativeMonth()](comparison#getrelativemonth)             | Returns the number of months between 2 dates.        |
    | [getRelativeYear()](comparison#getrelativeyear)               | Returns the number of years between 2 dates.         |
</details>

<details>
  <summary>[**Get & Set Methods**](get-set)</summary>
    | Method                                       | Short Description                                                       |
    | -------------------------------------------- | ----------------------------------------------------------------------- |
    | [get()](get-set#get)                         | Gets the value of a specific time unit from the date.                   |
    | [set()](get-set#set)                         | Returns a new instance with the specified unit set to the given value.  |
    | [startOf()](get-set#startof)                 | Returns a new `Chronos` instance at the start of a given unit.          |
    | [endOf()](get-set#endof)                     | Returns a new `Chronos` instance at the end of a given unit.            |
    | [getWeek()](get-set#getweek)                 | Calculates the ISO 8601 week number of the year.                        |
    | [setWeek()](get-set#setweek)                 | Sets the date to the specified ISO week number within the current year. |
    | [getWeekOfYear()](get-set#getweekofyear)     | Calculates the week number of the year based on custom week start.      |
    | [getWeekYear()](get-set#getweekyear)         | Returns the ISO week-numbering year for the current date.               |
    | [getDayOfYear()](get-set#getdayofyear)       | Returns day of year (1 - 366).                                          |
    | [getTimeStamp()](get-set#gettimestamp)       | Returns the time value in milliseconds since midnight, 1970-01-01 UTC.  |
    | [daysInMonth()](get-set#daysinmonth)         | Returns number of days in current month.                                |
    | [firstDayOfMonth()](get-set#firstdayofmonth) | Returns a new instance set to the first day of the current month.       |
    | [lastDayOfMonth()](get-set#lastdayofmonth)   | Returns a new instance set to the last day of the current month.        |
</details>

<details>
  <summary>[**String Methods**](strings)</summary>
    | Method                                         | Short Description                                                    |
    | ---------------------------------------------- | -------------------------------------------------------------------- |
    | [toString()](strings#tostring)                 | Returns a string representation of a date.                           |
    | [toISOString()](strings#toisostring)           | Returns a date as a string value in ISO format (UTC).                |
    | [toLocalISOString()](strings#tolocalisostring) | Returns ISO time string in appropriate time zone with offset.        |
    | [toLocaleString()](strings#tolocalestring)     | Wrapper over native JS `toLocaleString` with improved type system.   |
    | [toJSON()](strings#tojson)                     | Enables `JSON.stringify` to show readable output.                    |
    | [inspect()](strings#inspect)                   | Returns a debug-friendly string for `console.log` or `util.inspect`. |
</details>

<details>
  <summary>[**Extra Time Information**](extras)</summary>
    | Method                                                        | Short Description                                       |
    | ------------------------------------------------------------- | ------------------------------------------------------- |
    | [getUTCOffset()](extras#getutcoffset)                         | Returns the system's UTC offset formatted as `±HH:mm`   |
    | [getTimeZoneOffset()](extras#gettimezoneoffset)               | Returns the timezone offset in `±HH:mm` format          |
    | [getUTCOffsetMinutes()](extras#getutcoffsetminutes)           | Gets difference in mins between UTC and local system.   |
    | [getTimeZoneOffsetMinutes()](extras#gettimezoneoffsetminutes) | Returns the current instance's UTC offset in minutes.   |
    | [toQuarter()](extras#toquarter)                               | Returns the calendar quarter (1 to 4) of the instance.  |
    | [toFiscalQuarter()](extras#tofiscalquarter)                   | Returns the fiscal quarter based on custom start month. |
    | [toAcademicYear()](extras#toacademicyear)                     | Returns the academic year in format `YYYY-YYYY`.        |
</details>

<details>
  <summary>[**Static Methods**](statics)</summary>
    | Method                                     | Short Description                                                          |
    | ------------------------------------------ | -------------------------------------------------------------------------- |
    | [now()](statics#now)                       | Returns the number of milliseconds elapsed since midnight, 1970-01-01 UTC. |
    | [use()](statics#use)                       | Injects a plugin into the `Chronos` system.                                |
    | [register()](statics#register)             | Alias for [`use()`](statics#use)                                           |
    | [utc()](statics#utc)                       | Creates a UTC-based `Chronos` instance.                                    |
    | [with()](statics#with)                     | Creates a new `Chronos` instance with the provided time component(s).      |
    | [parse()](statics#parse)                   | Parses a date string with a given format (limited support only).           |
    | [max()](statics#max)                       | Returns latest Chronos.                                                    |
    | [min()](statics#min)                       | Returns earliest Chronos.                                                  |
    | [today()](statics#today)                   | Returns the current date and time in a specified format in local time.     |
    | [tomorrow()](statics#tomorrow)             | Returns a new `Chronos` instance representing tomorrow's date.             |
    | [yesterday()](statics#yesterday)           | Returns a new `Chronos` instance representing yesterday's date.            |
    | [formatTimePart()](statics#formattimepart) | Formats a time-only string into a formatted time string.                   |
    | [getDatesForDay()](statics#getdatesforday) | Returns ISO date strings for each occurrence of a weekday.                 |
    | [isLeapYear()](statics#isleapyear)         | Checks if the year in the date or year (from 0 - 9999) is a leap year.     |
    | [isValidChronos()](statics#isvalidchronos) | Checks if the given value is an instance of `Chronos`.                     |
    | [isValidDate()](statics#isvaliddate)       | Checks if the given value is a valid `Date` object.                        |
    | [isDateString()](statics#isdatestring)     | Checks if the given value is a valid date string.                          |
</details>

<details>
  <summary>[**Symbol Methods**](symbols)</summary>
    | Method                                                        | Short Description                                        |
    | ------------------------------------------------------------- | -------------------------------------------------------- |
    | [Symbol.iterator](symbols#symboliterator)                     | Returns the default iterator for an object.              |
    | [Symbol.toPrimitive](symbols#symboltoprimitive)               | Converts an object to a corresponding primitive value.   |
    | [Symbol.replace](symbols#symbolreplace)                       | `RegExp` method, replaces matched parts of a string.     |
    | [Symbol.search](symbols#symbolsearch)                         | `RegExp` method, returns index within a matched string.  |
    | [Symbol.split](symbols#symbolsplit)                           | `RegExp` method, splits a string at the matched indices. |
    | [Symbol.match](symbols#symbolmatch)                           | `RegExp` method, matches the `RegExp` against a string.  |
    | [Symbol.toStringTag](symbols#symboltostringtag)               | Called by the built-in `Object.prototype.toString`.      |
    | [Symbol.isConcatSpreadable](symbols#symbolisconcatspreadable) | Enables to be spread with the spread operator `...`      |
</details>
