---
title: Chronos
description: Chronos - Play with Time and Date, Be a Time Master like Chronos
icon: Clock
---

<Callout type="idea" title="About Chronos" >
In ancient Greek mythology, **Chronos** is the primordial embodiment of time — not merely tracking moments, but **defining their very existence**. Like its mythological namesake, the `Chronos` class offers **precise, immutable, and expressive control** over time within your application.

Designed to go beyond `Date`, it empowers you to manipulate, format, compare, and traverse time with **clarity, reliability, and confidence** — all while staying _immutable_ and _framework-agnostic_.

Whether you're building a calendar, a countdown, or scheduling logic, `Chronos` gives you the power to shape time as you see fit.
</Callout>

## API Reference for Chronos

This documentation provides a detailed guide to the `Chronos` class, a comprehensive date and time manipulation class. The methods are grouped into logical categories for easier navigation.

<Callout type="info" title="For chronos function">
For `chronos` function, a `Chronos` wrapper, refer to the [**Wrapper Function**](./chronos/wrapper-function)
</Callout>

### Import

```ts
import { Chronos } from 'chronos-date';
```

### Plugin System

Chronos supports a modular plugin system that allows you to extend its capabilities without bloating the core. Plugin methods are **not available by default**, you must explicitly install them using the `.use()` or `.register()` static method.

#### How it works

```ts pg
import { Chronos } from 'chronos-date';
import { seasonPlugin } from 'chronos-date/plugins/seasonPlugin';

// Register the plugin before using its methods
Chronos.use(seasonPlugin); 
// or
Chronos.register(seasonPlugin);

const now = new Chronos();
console.log(now.season()); // ✅ Safe to use after plugin registration
```

<Callout type="info" title="Info">
Each plugin enhances the `Chronos` prototype and becomes available globally after registration.
</Callout>

### Constructor Signatures

```ts
 constructor();
 constructor(value: number);
 constructor(value: string);
 constructor(value: Date);
 constructor(value: Chronos);
 constructor(year: number, month?: NumberRange<1, 12>, date?: NumberRange<1, 31>, hours?: Enumerate<24>, minutes?: Enumerate<60>, seconds?: Enumerate<60>, ms?: Milliseconds); // 
 constructor(year: number, month: number, date?: number, hours?: number, minutes?: number, seconds?: number, ms?: number);
 constructor(value?: ChronosInput);
```

#### ChronosInput

```ts
type ChronosInput = number | string | Date | Chronos;
```

### Public & Protected Properties

These properties provide non-destructive, read-only (only the core ones) access to the copies of internal states of a `Chronos` instance for debugging, inspection, or meta-awareness.

<Callout type="info" title="Info">
However, in JavaScript, these properties _can technically be mutated_ (Compile-time `Error` occurs in TypScript if these properties are tried to be mutated), but such mutations (changes) **do not** affect the `Chronos` instance itself. The class internally manages equivalent strict **readonly/private state**. These public properties exist _purely for developer convenience and sugar_.
</Callout>

#### native

Returns the underlying native JavaScript `Date` object used internally by the `Chronos` instance. This is useful for interoperability with APIs or libraries that expect a native `Date`.

<Callout type="info" title="Info">
Equivalent to [`toDate()`](./chronos/convert/to-date) instance method.
</Callout>

```ts pg
import { Chronos } from 'chronos-date';

const ch = new Chronos('2025-01-01');
console.log(ch.native.toISOString()); // → 2025-01-01T00:00:00.000Z
```

#### origin

Indicates how the current `Chronos` instance was created. This can be helpful for debugging flow logic or tracing method chains in complex date-time pipelines.

Possible values:

- `'root'`: if the instance was directly constructed
- A method name string such as `'addDays'`, `'startOf'`, etc., if the instance was produced as the result of a method call (which can create a new instance of `Chronos`). If no such method was called it shows the last previous method name as `origin`, if there is none, it shows `root`.

```ts pg
import { Chronos } from 'chronos-date';

const root = new Chronos();
const viaMethod = root.addDays(3);

console.log(root.origin); // → 'root'
console.log(viaMethod.origin); // → 'addDays'
```

#### utcOffset

Returns the current UTC offset in `UTC±HH:mm` format (e.g., `"UTC+06:00"`, `"UTC-05:30"`).

<Callout type="info" title="Info">

- Also accessible via [`getTimeZoneOffset()`](./chronos/offset/get-timezone-offset) instance method without `UTC` prefix (returns in `±HH:mm` format).
- This value is automatically updated when using timezone or UTC manipulation methods.

</Callout>

```ts pg
import { Chronos } from 'chronos-date';
import { timeZonePlugin } from 'chronos-date/plugins/timeZonePlugin';

Chronos.use(timeZonePlugin);

const ch = new Chronos('2025-01-01');
console.log(ch.utcOffset); // → "UTC+06:00" (Local Timezone Offset)

const ny = ch.timeZone('America/New_York');
console.log(ny.utcOffset); // → "UTC-05:00" (or UTC-04:00 during DST)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### timeZoneName

Represents the current timezone name (e.g., `"Bangladesh Standard Time"`), or falls back to the corresponding timezone identifier (e.g., `"Asia/Dhaka"`) if no name can be resolved.

<Callout type="info" title="Remarks">

- Invoking the [`timeZone()`](./plugins/timezone-plugin#timezone) method sets the timezone name that corresponds to the specified UTC offset, or the UTC offset itself if no name exists. For more details on this behavior, see [`getTimeZoneName()`](./plugins/timezone-plugin#gettimezonename).
- To retrieve the local system's native timezone name (or its identifier if the name is unavailable), use the [`$getNativeTimeZoneName()`](./chronos/get-set/get-native-timezone-name) instance method.

</Callout>

```ts pg
import { Chronos } from 'chronos-date';
import { timeZonePlugin } from 'chronos-date/plugins/timeZonePlugin';

Chronos.use(timeZonePlugin);

const ch = new Chronos('2025-01-01');
console.log(ch.timeZoneName); // → "Bangladesh Standard Time" (Local Timezone Name)

const custom = ch.timeZone('UTC+02:45');
console.log(custom.timeZoneName); // → "UTC+02:45" (no named timezone)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### timeZoneId

Represents the current timezone context, which can be a single identifier, an array of equivalent identifiers, or a UTC offset.

**Possible return types:**

- **`$TimeZoneIdentifier`** — e.g., `"Asia/Dhaka"`. Returned when the [`timeZone()`](./plugins/timezone-plugin#timezone) method has not been invoked (default behavior).
- **Array of `$TimeZoneIdentifier`** — e.g., `['Asia/Kathmandu', 'Asia/Katmandu']`, used when multiple timezones share the same UTC offset such as `"UTC+05:45"`.
- **`UTCOffset`** — e.g., `"UTC+06:45"` or `"UTC+02:15"`, returned when no named timezone corresponds to a given offset.

<Callout type="info" title="Remarks">

- By default, when [`timeZone()`](./plugins/timezone-plugin#timezone) is not applied, a single `$TimeZoneIdentifier` string is provided.
- When applied, it may instead return a single identifier string, an array of equivalent identifiers, or a UTC offset string.
- To retrieve the local system's native timezone identifier, use the [`$getNativeTimeZoneId()`](./chronos/get-set/get-native-timezone-id) instance method.

</Callout>

```ts pg
import { Chronos } from 'chronos-date';
import { timeZonePlugin } from 'chronos-date/plugins/timeZonePlugin';

Chronos.use(timeZonePlugin);

const ch = new Chronos('2025-01-01');
console.log(ch.timeZoneId); // → "Asia/Dhaka" (system timezone)

const multi = ch.timeZone('UTC+05:30');
console.log(multi.timeZoneId); 
// → [ 'Asia/Calcutta', 'Asia/Colombo' ] (multiple equivalent zones)

const offsetOnly = ch.timeZone('UTC+02:15');
console.log(offsetOnly.timeZoneId); // → "UTC+02:15" (no named timezone)
```

<Callout type="error" title="Caution">
This property is mutable, but modifying it has no effect on the `Chronos` instance. Any logic that relies on the mutated value may behave incorrectly, as `Chronos` internally derives its actual state from private fields.
</Callout>

#### $tzTracker

<Callout type="error" title="Warning">
**Protected** Internal flag used to track instances created via the [`timeZone()`](./plugins/timezone-plugin#timezone) method. This value participates in the internal resolution of time-zone name, identifier, and offset. _Although it is protected_, it can still be mutated in JavaScript/TypeScript; and doing so may de-synchronize these time-zone properties and lead to inconsistent results. _Avoid modifying this field unless you fully understand the implications_.
</Callout>

```ts pg
import { Chronos } from 'chronos-date';
import { timeZonePlugin } from 'chronos-date/plugins/timeZonePlugin';

Chronos.use(timeZonePlugin);

// Can access but TypeScript may complain

const ch = new Chronos('2025-01-01');
// @ts-ignore: Protected method but still accessible
console.log(ch.$tzTracker); // → undefined

const custom = ch.timeZone('Asia/Kolkata');
// @ts-ignore: Protected method but still accessible
console.log(custom.$tzTracker); // → "Asia/Kolkata"
```

### Available Methods

<Accordions type="multiple">
  <Accordion title="Getter Methods">
    | Method                                               | Short Description                                                      |
    | ---------------------------------------------------- | ---------------------------------------------------------------------- |
    | [year](./chronos/getters#year)                       | Gets the full year of the date.                                        |
    | [month](./chronos/getters#month)                     | Gets the month (0-11) of the date.                                     |
    | [date](./chronos/getters#date)                       | Gets the day of the month (1-31).                                      |
    | [weekDay](./chronos/getters#weekday)                 | Gets the day of the week (0-6, where 0 is Sunday).                     |
    | [hour](./chronos/getters#hour),                      | Gets the hour (0-23) of the date.                                      |
    | [minute](./chronos/getters#minute)                   | Gets the minute (0-59) of the date.                                    |
    | [second](./chronos/getters#second)                   | Gets the second (0-59) of the date.                                    |
    | [millisecond](./chronos/getters#millisecond)         | Gets the millisecond (0-999) of the date.                              |
    | [isoWeekDay](./chronos/getters#isoweekday)           | Gets ISO weekday: 1 = Monday, 7 = Sunday.                              |
    | [isoMonth](./chronos/getters#isomonth)               | Gets ISO month (1–12 instead of 0–11).                                 |
    | [unix](./chronos/getters#unix)                       | Returns the Unix timestamp (seconds since the Unix epoch).             |
    | [timestamp](./chronos/getters#timestamp)             | Gets the time value in milliseconds since midnight, 1970-01-01 UTC.    |
    | [lastDateOfMonth](./chronos/getters#lastdateofmonth) | Gets the last date (number) of the current month `(28, 29, 30 or 31)`. |
  </Accordion>

  <Accordion title="Format Methods">
    | Method                                                     | Short Description                                                         |
    | ---------------------------------------------------------- | ------------------------------------------------------------------------- |
    | [format()](./chronos/format/format)                        | Formats the current date into a custom string format.                     |
    | [formatSafe()](./chronos/format/format-safe)               | Formats the date into a predefined strict string format.                  |
    | [formatUTC()](./chronos/format/format-utc)                 | Formats the date into a custom string format (UTC time).                  |
    | [calendar()](./chronos/format/calendar)                    | Returns a human-readable relative calendar time like `"Today at 3:00 PM"` |
    | [fromNow()](./plugins/from-now-plugin#fromnow)             | Returns full time difference as string down to a given level.             |
    | [fromNowShort()](./chronos/format/from-now-short)          | Returns a short human-readable string like "2h ago", "in 5m".             |
    | [getGreeting()](./plugins/greeting-plugin#getgreeting)     | Returns a greeting message based on `Chronos` instance or provided time.  |
    | [greet()](./plugins/greeting-plugin#alias)                 | Alias for [`getGreeting()`](./plugins/greeting-plugin#getgreeting).       |
    | [toISOString()](./chronos/format/to-iso-string)            | Returns a date as a string value in ISO format (UTC).                     |
    | [toLocalISOString()](./chronos/format/to-local-iso-string) | Returns ISO time string in appropriate time zone with offset.             |
    | [toLocaleString()](./chronos/format/to-locale-string)      | Wrapper over native JS `toLocaleString` with improved type system.        |
    | [toString()](./chronos/format/to-string)                   | Returns a string representation of the date and time.                     |
    | [toDateString()](./chronos/format/to-date-string)          | Returns a string representation of the date portion of the date.          |
    | [toTimeString()](./chronos/format/to-time-string)          | Returns a string representation of the time portion of the date.          |
    | [toUTCString()](./chronos/format/to-utc-string)            | Returns a string representation of the date and time in UTC.              |
  </Accordion>

  <Accordion title="Manipulation Methods">
    | Method                                                       | Short Description                                                     |
    | ------------------------------------------------------------ | --------------------------------------------------------------------- |
    | [clone()](./chronos/manipulate/clone)                        | Clones and returns exactly same `Chronos` instance.                   |
    | [next()](./chronos/manipulate/next)                          | Returns a new `Chronos` instance advanced by the specified time unit. |
    | [previous()](./chronos/manipulate/previous)                  | Returns a new `Chronos` instance moved back by one time unit.         |
    | [startOf()](./chronos/manipulate/start-of)                   | Returns a new `Chronos` instance at the start of a given unit.        |
    | [endOf()](./chronos/manipulate/end-of)                       | Returns a new `Chronos` instance at the end of a given unit.          |
    | [add()](./chronos/manipulate/add)                            | Returns a new `Chronos` instance with the specified unit added.       |
    | [addDays()](./chronos/manipulate/add-days)                   | Adds days and returns a new immutable instance.                       |
    | [addHours()](./chronos/manipulate/add-hours)                 | Adds hours and returns a new immutable instance.                      |
    | [addMinutes()](./chronos/manipulate/add-minutes)             | Adds minutes and returns a new immutable instance.                    |
    | [addMonths()](./chronos/manipulate/add-months)               | Adds months and returns a new immutable instance.                     |
    | [addSeconds()](./chronos/manipulate/add-seconds)             | Adds seconds and returns a new immutable instance.                    |
    | [addWeeks()](./chronos/manipulate/add-weeks)                 | Adds weeks and returns a new immutable instance.                      |
    | [addYears()](./chronos/manipulate/add-years)                 | Adds years and returns a new immutable instance.                      |
    | [subtract()](./chronos/manipulate/subtract)                  | Returns a new `Chronos` instance with the specified unit subtracted.  |
    | [round()](./plugins/round-plugin#round)                      | Rounds the current date-time to the nearest unit and interval.        |
    | [firstDayOfMonth()](./chronos/manipulate/first-day-of-month) | Returns a new instance set to the first day of the current month.     |
    | [lastDayOfMonth()](./chronos/manipulate/last-day-of-month)   | Returns a new instance set to the last day of the current month.      |
  </Accordion>

  <Accordion title="Query/Checker Methods">
    | Method                                                       | Short Description                                                    |
    | ------------------------------------------------------------ | -------------------------------------------------------------------- |
    | [isAfter()](./chronos/query/is-after)                        | Checks if this date is after another date in a specific unit.        |
    | [isBefore()](./chronos/query/is-before)                      | Checks if this date is before another date in a specific unit.       |
    | [isBetween()](./chronos/query/is-between)                    | Checks if the current date is between the given start and end dates. |
    | [isSame()](./chronos/query/is-same)                          | Checks if another date is the same as this one in a specific unit.   |
    | [isSameOrAfter()](./chronos/query/is-same-or-after)          | Checks if this date is same or after another in a specific unit.     |
    | [isSameOrBefore()](./chronos/query/is-same-or-before)        | Checks if this date is same or before another in a specific unit.    |
    | [isEqual()](./chronos/query/is-equal)                        | Checks if another date is exactly equal to this one.                 |
    | [isEqualOrAfter()](./chronos/query/is-equal-or-after)        | Checks if another date is exactly equal to or after this one.        |
    | [isEqualOrBefore()](./chronos/query/is-equal-or-before)      | Checks if another date is exactly equal to or before this one.       |
    | [isBusinessHour()](./plugins/business-plugin#isbusinesshour) | Checks if the current date and time fall within business hours.      |
    | [isWeekend()](./plugins/business-plugin#isweekend)           | Checks if the current date falls on a weekend.                       |
    | [isWorkday()](./plugins/business-plugin#isworkday)           | Checks if the current date is a workday.                             |
    | [isFirstDayOfMonth()](./chronos/query/is-first-day-of-month) | Checks if current day is the first day of the current month.         |
    | [isLastDayOfMonth()](./chronos/query/is-last-day-of-month)   | Checks if current day is the last day of the current month.          |
    | [isToday()](./plugins/relative-time-plugin#istoday)          | Checks if the current date is today.                                 |
    | [isTomorrow()](./plugins/relative-time-plugin#istomorrow)    | Checks if the current date is tomorrow.                              |
    | [isYesterday()](./plugins/relative-time-plugin#isyesterday)  | Checks if the current date is yesterday.                             |
    | [isLeapYear()](./chronos/query/is-leap-year)                 | Checks if the current year is a leap year.                           |
    | [isPalindromeDate()](./plugins/palindrome-plugin)            | Checks if the current date (date part only) is a palindrome.         |
    | [isDST()](./chronos/query/is-dst)                            | Checks if the date is within daylight saving time (DST).             |
  </Accordion>

  <Accordion title="Conversion Methods">
    | Method                                           | Short Description                                                            |
    | ------------------------------------------------ | ---------------------------------------------------------------------------- |
    | [toUTC()](./chronos/convert/to-utc)              | Returns new `Chronos` instance in UTC time.                                  |
    | [toLocal()](./chronos/convert/to-local)          | Returns new `Chronos` instance in local time.                                |
    | [timeZone()](./plugins/timezone-plugin#timezone) | Creates a new instance of `Chronos` for the specified time zone information. |
    | [toDate()](./chronos/convert/to-date)            | Gets the native `Date` instance.                                             |
    | [toArray()](./chronos/convert/to-array)          | Converts to array with all date unit part                                    |
    | [toObject()](./chronos/convert/to-object)        | Converts to object with all date unit parts                                  |
  </Accordion>

  <Accordion title="Comparison Methods">
    | Method                                                                            | Short Description                                |
    | --------------------------------------------------------------------------------- | ------------------------------------------------ |
    | [compare()](./plugins/relative-time-plugin#compare)                               | Returns difference of 2 dates in specified unit. |
    | [getRelativeDay()](./plugins/relative-time-plugin#getrelativeday)                 | Returns number of days between 2 dates.          |
    | [getRelativeHour()](./plugins/relative-time-plugin#getrelativehour)               | Returns number of hours between 2 dates.         |
    | [getRelativeMinute()](./plugins/relative-time-plugin#getrelativeminute)           | Returns number of minutes between 2 dates.       |
    | [getRelativeSecond()](./plugins/relative-time-plugin#getrelativesecond)           | Returns number of seconds between 2 dates.       |
    | [getRelativeMilliSecond()](./plugins/relative-time-plugin#getrelativemillisecond) | Returns number of milliseconds between 2 dates.  |
    | [getRelativeWeek()](./plugins/relative-time-plugin#getrelativeweek)               | Returns number of weeks between 2 dates.         |
    | [getRelativeMonth()](./plugins/relative-time-plugin#getrelativemonth)             | Returns number of months between 2 dates.        |
    | [getRelativeYear()](./plugins/relative-time-plugin#getrelativeyear)               | Returns number of years between 2 dates.         |
  </Accordion>

  <Accordion title="Get & Set Methods">
    | Method                                                                   | Short Description                                                    |
    | ------------------------------------------------------------------------ | -------------------------------------------------------------------- |
    | [get()](./chronos/get-set/get)                                           | Gets the value of a specific time unit from the date.                |
    | [set()](./chronos/get-set/set)                                           | Returns new instance with the specified unit set to the given value. |
    | [diff()](./chronos/get-set/diff)                                         | Returns the difference between current and another date.             |
    | [getWeek()](./chronos/get-set/get-week)                                  | Calculates the ISO 8601 week number of the year.                     |
    | [setWeek()](./chronos/get-set/set-week)                                  | Sets date to the specified ISO week number within the current year.  |
    | [getWeekOfYear()](./chronos/get-set/get-week-of-year)                    | Calculates the week number of the year based on custom week start.   |
    | [getWeekYear()](./chronos/get-set/get-week-year)                         | Returns the ISO week-numbering year for the current date.            |
    | [getDayOfYear()](./chronos/get-set/get-day-of-year)                      | Returns day of year (1 - 366).                                       |
    | [getTimeStamp()](./chronos/get-set/get-timestamp)                        | Returns time value in milliseconds since midnight, 1970-01-01 UTC.   |
    | [daysInMonth()](./chronos/get-set/days-in-month)                         | Returns number of days in current month.                             |
    | [getDatesInRange()](./plugins/date-range-plugin#getdatesinrange)         | Generates a list of ISO date strings within a specified range.       |
    | [getQuarter()](./chronos/get-set/get-quarter)                            | Returns calendar quarter (1 to 4) of the instance.                   |
    | [getFiscalQuarter()](./plugins/business-plugin#getfiscalquarter)         | Returns the fiscal quarter based on custom start month.              |
    | [getAcademicYear()](./plugins/business-plugin#getacademicyear)           | Returns the academic year in format `YYYY-YYYY`.                     |
    | [duration()](./plugins/duration-plugin#duration)                         | Returns full time duration between two dates.                        |
    | [durationString()](./plugins/duration-plugin#durationstring)             | Returns a human-readable formatted duration string.                  |
    | [day()](./chronos/get-set/day)                                           | Gets name of the current day or optional day index.                  |
    | [monthName()](./chronos/get-set/month-name)                              | Gets name of the current month or optional month index.              |
    | [getTimeZoneName()](./plugins/timezone-plugin#gettimezonename)           | Gets current time zone name in descriptive string.                   |
    | [getTimeZoneNameShort()](./plugins/timezone-plugin#gettimezonenameshort) | Gets current time zone abbreviation (e.g. `"BST"`).                  |
    | [getTimeZoneNameAbbr()](./plugins/timezone-plugin#gettimezonenameshort)  | Gets time zone abbreviation (`getTimeZoneNameShort()`).              |
    | [season()](./plugins/season-plugin#season)                               | Gets current season name based on options.                           |
    | [getSeasonName()](./plugins/season-plugin#season)                        | Alias for `season()`.                                                |
    | [getZodiacSign()](./plugins/zodiac-plugin#getzodiacsign)                 | Gets zodiac sign based on current date or `birthDate`.               |
    | [zodiac()](./plugins/zodiac-plugin#getzodiacsign)                        | Alias for `getZodiacSign()`.                                         |
    | [getPartOfDay()](./plugins/day-part-plugin#getpartofday)                 | Returns the part of day based on the current hour.                   |
    | [$getNativeTimeZoneName()](./chronos/get-set/get-native-timezone-name)   | Retrieves the local system's current timezone name.                  |
    | [$getNativeTimeZoneId()](./chronos/get-set/get-native-timezone-id)       | Retrieves the IANA time zone identifier for the local system.        |
  </Accordion>

  <Accordion title="Protocol Methods">
    | Method                                   | Short Description                                                    |
    | ---------------------------------------- | -------------------------------------------------------------------- |
    | [valueOf()](./chronos/protocol/value-of) | Enables arithmetic and comparison operations (e.g., +new Chronos()). |
    | [toJSON()](./chronos/protocol/to-json)   | Enables `JSON.stringify` to display readable output.                 |
    | [inspect()](./chronos/protocol/inspect)  | Returns a debug-friendly string for `console.log` or `util.inspect`. |
  </Accordion>

  <Accordion title="Offset Extraction Methods">
    | Method                                                                     | Short Description                                     |
    | -------------------------------------------------------------------------- | ----------------------------------------------------- |
    | [getUTCOffset()](./chronos/offset/get-utc-offset)                          | Returns the system's UTC offset formatted as `±HH:mm` |
    | [getTimeZoneOffset()](./chronos/offset/get-timezone-offset)                | Returns the timezone offset in `±HH:mm` format        |
    | [getUTCOffsetMinutes()](./chronos/offset/get-utc-offset-minutes)           | Gets difference in mins between UTC and local system. |
    | [getTimeZoneOffsetMinutes()](./chronos/offset/get-timezone-offset-minutes) | Returns the current instance's UTC offset in minutes. |
  </Accordion>

  <Accordion title="Static Methods">
    | Method                                                   | Short Description                                                      |
    | -------------------------------------------------------- | ---------------------------------------------------------------------- |
    | [now()](./chronos/static/now)                            | Get the number of milliseconds elapsed since midnight, 1970-01-01 UTC. |
    | [use()](./chronos/static/use)                            | Injects a plugin into the `Chronos` system.                            |
    | [register()](./chronos/static/register)                  | Alias for [`use()`](./chronos/static/use)                              |
    | [utc()](./chronos/static/utc)                            | Creates a UTC-based `Chronos` instance.                                |
    | [with()](./chronos/static/with)                          | Creates a new `Chronos` instance with the provided time component(s).  |
    | [reconstruct](./chronos/static/reconstruct)              | Reconstructs a `Chronos` instance from a given value.                  |
    | [parse()](./chronos/static/parse)                        | Parses a date string with a given format (limited support only).       |
    | [min()](./chronos/static/min)                            | Returns earliest `Chronos`.                                            |
    | [max()](./chronos/static/max)                            | Returns latest `Chronos`.                                              |
    | [clamp()](./chronos/static/clamp)                        | Restricts a date to a specified range                                  |
    | [today()](./chronos/static/today)                        | Returns the current date and time in a specified format in local time. |
    | [tomorrow()](./chronos/static/tomorrow)                  | Returns a new `Chronos` instance representing tomorrow's date.         |
    | [yesterday()](./chronos/static/yesterday)                | Returns a new `Chronos` instance representing yesterday's date.        |
    | [formatTimePart()](./chronos/static/format-time-part)    | Formats a time-only string into a formatted time string.               |
    | [getDatesForDay()](./chronos/static/get-dates-for-day)   | Returns ISO date strings for each occurrence of a weekday.             |
    | [isLeapYear()](./chronos/static/is-leap-year)            | Checks if the year in the date or year (from 0 - 9999) is a leap year. |
    | [isValidChronos()](./chronos/static/is-valid-chronos)    | Checks if the given value is an instance of `Chronos`.                 |
    | [isValidDate()](./chronos/static/is-valid-date)          | Checks if the given value is a valid `Date` object.                    |
    | [isDateString()](./chronos/static/is-date-string)        | Checks if the given value is a valid date string.                      |
    | [isReconstructable](./chronos/static/is-reconstructable) | Checks if a value can be used to reconstruct a `Chronos` instance.     |
  </Accordion>

  <Accordion title="Symbol Methods">
    | Method                                                                  | Short Description                                        |
    | ----------------------------------------------------------------------- | -------------------------------------------------------- |
    | [Symbol.iterator](./chronos/symbols#symboliterator)                     | Returns the default iterator for an object.              |
    | [Symbol.toPrimitive](./chronos/symbols#symboltoprimitive)               | Converts an object to a corresponding primitive value.   |
    | [Symbol.replace](./chronos/symbols#symbolreplace)                       | `RegExp` method, replaces matched parts of a string.     |
    | [Symbol.search](./chronos/symbols#symbolsearch)                         | `RegExp` method, returns index within a matched string.  |
    | [Symbol.split](./chronos/symbols#symbolsplit)                           | `RegExp` method, splits a string at the matched indices. |
    | [Symbol.match](./chronos/symbols#symbolmatch)                           | `RegExp` method, matches the `RegExp` against a string.  |
    | [Symbol.toStringTag](./chronos/symbols#symboltostringtag)               | Called by the built-in `Object.prototype.toString`.      |
    | [Symbol.isConcatSpreadable](./chronos/symbols#symbolisconcatspreadable) | Enables to be spread with the spread operator `...`      |
  </Accordion>
</Accordions>
